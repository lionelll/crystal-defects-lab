# 晶体缺陷本地开发与共用界面验收

## 开始修改前

先阅读材料项目根目录和本工作区的 `AGENTS.md`、本项目 PRD，以及公共 `四项目共用页面外壳规范.md`、`统一Design-QA规范.md` 和 `品牌素材说明.md`。这些文件是本项目处理共用页面区域的目标和验收依据；其他项目仓库仅可只读参考。用户已明确本项目今后在 `dev` 开发，`release` 用于发布，`main` 同步已验收的 `release` 代码；正式版本号和服务器路径仍须按当次任务确认。

## 本项目分支职责

- `dev`：日常功能、缺陷修复、测试和配套文档；当前本地工作树检出此分支。
- `release`：只接收经批准准备发布的 `dev` 精确提交；具体发布由 `10-部署发布` 按授权执行。
- `main`：正式发布并验收后同步 `release` 的精确代码，不直接承接日常开发。
- 分支同步、提交、推送和部署各需相应当次授权；本分支约定不改变其他三个项目的规则。与材料项目根目录的通用 `feature → dev → release → main` 文字差异，已在本项目版本记录中移交 `00-架构师` 协调。

## 共用参数是开发门禁

改动顶栏、品牌、左右面板、选择卡或响应式外壳时，先从公共规范列出适用参数，再改本项目代码。不能只看视觉相近、只复制变量名，或靠额外 `gap`、根节点缩放等方式凑出局部位置。完成后在相同 CSS 视口、DPR、浏览器缩放和等效状态下比较**最终计算样式、布局矩形和截图**；把来源、修正值、控制台、资源、横向溢出及未关闭项记入 `design-qa.md`。缺稳定截图时不得宣称完整视觉验收通过。

用户确认正确的晶体结构顶栏参考图为 `docs/qa/design-qa-brand-spacing-approved-reference.png`。本项目顶栏按晶体结构的块级排版：`div.brand-mark` 网格居中，文字容器为普通 `div`，内部是 `h1` 和 `display:block` 副标题。桌面顶栏高 96px、Logo 布局 54×54px 且图像缩放 1.55、Logo 与文字水平间距 14px；标题 25px/700/1.1，副标题 13px/600，副标题自身 `margin-top:7px`，文字容器不再叠加行间 `gap`。本地 1567×900 视口的晶体结构与晶体缺陷对应矩形已记录在 `design-qa.md`。这些数值若由“00-架构师”更新，以届时最新公共规范为准并重新验收。

≤820px 与 ≤520px 的品牌和按钮尺寸、三栏转两栏再转单栏的规则，以及 Logo、面板和选择卡目标值，以公共页面外壳规范为准。位错晶格、阶段动画和底部时间轴属于本项目业务区域，单独验证科学几何与交互，不拿它们的特有尺寸改写共用外壳参数。

## 本地验证

```bash
npm install
npm test
npm run build
npm run dev -- --port 4173
```

本地预览路径为 `/defects/`。构建与浏览器自测不代表获准创建 Git 版本、提交、推送或部署；正式和测试环境路径尚未登记。
