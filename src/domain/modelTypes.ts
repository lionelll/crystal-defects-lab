export interface DisplayOptions {
  atoms: boolean;
  lattice: boolean;
  line: boolean;
  burgers: boolean;
  plane: boolean;
  trajectory: boolean;
  extraHalfPlane: boolean;
  stress: boolean;
  surfaceStep: boolean;
}
