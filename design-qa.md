# 晶体缺陷本地 Design QA

> 2026-09-24 独立自查补修：位错线图例随开关隐藏；手机宽度时间轴可在观看模型时操作；静态/暂停状态改按需渲染。`npm test` 41/41、构建通过，15 场景及关键交互浏览器复核见工作区《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末。目标设备性能与科学验收仍未完成。

> 2026-09-24 最新 R1 / N1 / N2 复核：静态螺型表面台阶新会话默认开启，螺型滑移默认关闭且两场景各自记忆；521–1280px 时间轴固定在视口底部；滑移面与层错面图例按实际几何显示。39/39 测试、构建和 15 场景定点浏览器检查通过。详见工作区《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末；下方旧默认值按记录时点理解。

> 2026-09-24 最新默认值：新会话中切应力和表面台阶均关闭；刃型滑移默认 5/8、螺型滑移默认 4/8。螺型教学提示要求按需打开台阶。`npm test` 39/39、构建及 1440×900/1280×720 定点浏览器复核通过。完整记录见《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末；下方旧默认值和测试数按记录时点理解。

> 2026-09-24 最新阶段与开关复核：刃型滑移初段显示“滑移起始”，交滑移提示打开伯氏矢量；切应力跨适用场景保留用户选择。`npm test` 38/38、构建通过。定点浏览器核对见《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末。

> 2026-09-24 最新默认显示复核：两种滑移场景各 8 个可见显示开关中默认开启 5 个；切应力和伯氏矢量按需开启，螺型表面台阶保持可见。`npm test` 36/36、构建通过。详见《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末；下方旧测试数量按记录时点理解。

> 2026-09-24 显示控制与交互二次修复：切应力、表面台阶和额外半原子面原子各可独立显示；交割轨迹图例与默认值已对齐；Frank–Read 进度条 100% 保持本轮；交割重复按钮已合并。`npm test` 33/33 与构建通过，定点浏览器复核见《晶体缺陷PRD审计问题修复记录-2026-09-24.md》文末。下方先前 32 项测试等记录按当时版本理解。

> 2026-09-24 PRD 审计后的代码修复见本项目《晶体缺陷PRD审计问题修复记录-2026-09-24.md》：当前 32 项测试通过；切应力、表面台阶、空位接触等为教学示意，未完成学科签收或完整截图对照。

> 2026-09-24 动画更新与运动方向显示优化见本项目《晶体缺陷动画更新与可读性优化-2026-09-24.md》：当前 30 项测试通过，构建通过；资源复用已验证，但目标设备 FPS、内存与首屏耗时仍未量测。

> 材料学几何已在 2026-09-23 再修正：FCC A/B/C 密排、两部分位错位移、交割面内路径、固定交滑移面、Frank–Read 同面环和 Frank 层错标记已更新；双交滑移主体线受三面约束。详见本项目《晶体缺陷材料学修正与复核-2026-09-23.md》。下文较早的 24/26 项测试和几何参数记录应按其原检查时点理解；当前领域测试为 29/29 通过，完整科学及视觉验收仍未完成。

> 2026-09-23 后续功能修复已发生：下文 24 项测试、双交滑移 `.80` 出环、扩展位错 `.25` 后即可调间距等记录属于修复前检查。当前以本项目 `文档/02-设计与验收/晶体缺陷PRD缺陷修复与复核记录-2026-09-23.md` 为准：26 项测试通过，双交滑移 `.78–.86` 连接弧闭合、`.86` 后脱环，扩展位错 `.5` 后启用间距滑块，Frank–Read 可跨周期继续增殖。完整科学与视觉验收仍未完成。

## 结论与基准

当前是未确认版本号的本地开发稿，**尚未通过完整 Design QA 或科学验收**。来源为工作区 PRD `文档/01-产品需求/当前版本/晶体缺陷可视化实验室PRD文档【2026.9.23.1】.docx`、公共 `四项目共用页面外壳规范.md`、`统一Design-QA规范.md`、公共品牌 Logo，以及用户关于四项目相同区域须完全一致的明确要求。视觉参考副本为 `docs/qa/design-qa-shared-ui-reference.png`，与晶体结构项目索引中的 1920×1080 截图 SHA-256 一致。

页面本地地址 `http://127.0.0.1:4173/defects/`。`/defects/` 只用于本机联调，测试和正式服务器路由尚未登记。当前没有 Git 提交号可作为版本基准。

## 顶栏品牌文字间距纠错

用户最初指出问题的局部截图已原样归档为 `docs/qa/design-qa-brand-spacing-request.png`；随后用户确认正确的晶体结构顶栏截图已原样归档为 `docs/qa/design-qa-brand-spacing-approved-reference.png`（2546×102，SHA-256 `cb58bdd8d8d121248e81b60005e5e40d55e6a81c8b6c730eb2548d25b9ddcf65`）。晶体结构项目 `src/styles.css` 的品牌区采用 `gap:14px`（Logo 与文字）、Logo 布局 `54×54px` 且图像放大 `1.55`、标题 `25px / 700 / line-height:1.1`、副标题 `13px / 600` 且 `margin-top:7px`；文字容器没有额外的行间 `gap`。

本项目先前在文字容器设置 `display:flex; flex-direction:column; gap:4px`，又给副标题设置 `margin-top:7px`，使实际空隙变为 `11px`。第一次仅把 `gap` 改为 0，虽然测得 7px，用户仍指出 Logo 位置有别。本轮继续把 Logo 容器改为与晶体结构相同的 `div.brand-mark`，文字容器恢复普通块级 `div`，副标题明确 `display:block; margin-top:7px`，不再靠 flex 排版得到偶然相同的数值。

随后只读使用晶体结构仓库已有的 `dist/` 构建产物，在本机临时服务与晶体缺陷页同设 **1567×900 CSS 视口**，读取两个页面的最终布局矩形。两者的相同区域现在逐项重合：

| 顶栏元素 | 晶体结构 | 晶体缺陷 |
|---|---:|---:|
| 顶栏外框 `(x,y,w,h)` | `(10,10,1547,96)` | `(10,10,1547,96)` |
| Logo 布局容器 `(x,y,w,h)` | `(35,31,54,54)` | `(35,31,54,54)` |
| 放大后的 Logo 图片 `(x,y,w,h)` | `(20.15,16.15,83.7,83.7)` | `(20.15,16.15,83.7,83.7)` |
| 主标题 `(x,y,h)` | `(103,31.5,27.5)` | `(103,31.5,27.5)` |
| 副标题 `(x,y,h)` | `(103,66,18.5)` | `(103,66,18.5)` |
| Logo 容器到主标题水平间距 | `14px` | `14px` |
| 主标题到副标题实际纵向间距 | `7px` | `7px` |

两个页面的 Logo 运行时文件与公共源文件 SHA-256 相同，图像 `scale(1.55)`。主标题 `25px / 700 / line-height:1.1`，副标题 `13px / 600`；≤820px 标题为 17px，≤520px 副标题为 11px，仍保留 7px 上边距。这里的坐标来自同视口浏览器计算矩形，不把不同文字内容导致的标题宽度差当成位置差。

本项目实现图已在浏览器目视检查；当前浏览器截图工具未提供获准的本地保存路径，故尚无稳定的 `design-qa-brand-spacing-implementation.png`，不把这项局部修复误记为完整截图对照验收。

## 公共视觉基线复核

2026-09-23 在 1440×900、DPR 1、浏览器缩放 100% 的本地页面读取计算样式。新公共规范已将同级选择卡目标修订为**最小高度 54px**；此前记录的 56px 是旧规范值。

| 公共表面 | 最新目标 | 当前计算样式/结构 |
|---|---|---|
| 页面外框 | 留白 10px、工作区间距 12px | 同值；无根节点缩放 |
| 字体栈 | Inter 起始的公共栈 | `src/shared-lab-theme.css` 已声明；浏览器将 BlinkMacSystemFont 规范化为 system-ui |
| 顶部品牌 | 96px；Logo 54px × 1.55；标题 h1、25px / 700，前缀“材科基” | 同值；Logo `alt=""` |
| 顶部操作 | 高 48px、文字 16px、图标 28px、间距 14px | 同值 |
| 桌面三栏 | `clamp(290px,20vw,400px) minmax(520px,1fr) clamp(340px,24vw,470px)` | 1440px 下计算为 `290px / 760.4px / 345.6px` |
| 中央标题 | 高 68px、左右 26px、Logo 34px × 1.45、标题 18px / 800 | 同值 |
| 面板标题 | 高 54px、左右 20px、标题 17px / 800 | 同值 |
| 一级模块 | 高 54px、文字 16px、图标 22px、图标列 28px、间距 12px | 同值 |
| 同级选择卡 | 最小高 54px、文字 14px / 780、图标 23px、间距 10px、圆角 7px | 同值 |

**已修正的配色偏差**：此前把 PRD 的“明亮”误解释为整页浅色，忽略了三个已登记测试页共同使用的深色实验室主题。本轮只读查看 `http://123.57.11.145:8080/crystal/`、`/ternary/`、`/binary/` 的 1440×900 页面与计算样式后，新增 `src/shared-lab-theme.css` 对齐共同外壳。用户最新要求与 PRD 原文的差异详见工作区 `文档/02-设计与验收/晶体缺陷视觉规范差异记录.md`。

| 页面表面 | 三项目当前计算值 | 晶体缺陷当前计算值 |
|---|---|---|
| 页面外壳 | `#050b13 → #08121f → #06101c` 深色渐变 | 同值 |
| 顶部栏底色/边框 | `rgba(7,13,22,.9)` / `rgba(115,152,196,.48)` | 同值 |
| 面板渐变/边框 | `rgba(25,37,51,.88/.90) → rgba(14,24,35,.88/.90)` / `rgba(139,164,194,.25)` | `.88 → .88` / 同值 |
| 三维舞台 | 晶体结构 `rgba(5,13,22,.92)`，Three.js 清屏 `#05070c`；三元相图 `#07101b` | CSS 舞台 `rgba(5,13,22,.92)`；Three.js 清屏 `#05070c` |
| 主文字 | 晶体结构/二元相图 `#edf5ff` | 同值 |

顶栏已改为左侧完整实验室标题与右侧操作；左右侧栏改为与参考页相同的独立面板与 12px 间距。底部动画时间轴是位错场景特有功能，保留为业务差异。手机宽度下标题与导航文字缩小，优先维持可读和无横向溢出。

## 浏览器与功能检查

| 视口 | 当前检查结果 |
|---|---|
| 1440×900，DPR 1，100% | 深色三栏、独立左右面板、顶部和播放栏可见；背景计算值见上表，无横向溢出。 |
| 1280×900，DPR 1，100% | 左栏 270px 加中央栏，右栏进入下一行；无横向溢出。 |
| 820×900，DPR 1，100% | 控制区、画布、信息区单列顺序；顶部三个同类操作等宽；无横向溢出。 |
| 520×900，DPR 1，100% | Logo 34px、操作图标 18px、操作文字 12px、一级模块文字 16px；无横向溢出。 |
| 390×900，DPR 1，100% | 维持单列；无横向溢出。 |

- 五模块、15 场景逐项选择并进入中间阶段；关键场景保留 3D 旋转、播放、暂停、重置与进度拖动。
- `npm test`：24 项领域约束测试通过；`npm run build`：TypeScript 与 Vite 构建通过。
- Frank–Read 两个脱环边界 `.385/.865` 前后源线均保持 9 点且坐标最大跳变小于 `.01`，新环从接触构型透明渐显；本地浏览器核对了弓出和首环脱离画面。双交滑移初态线方向平行于 `b`，第二次转面完成于 `.78`，候选环从 `.80` 后出现；浏览器在 `.75` 仍显示“第二次交滑移”，`.85` 显示“环形成”。
- Frank 不全位错显示缺失的 `(111)` 原子圆盘与上方层的法向闭合，界面明确标注教学构型而非原子级弛豫。扩展位错在完整线阶段禁用间距滑块并显示原因；进入分解阶段后滑块启用并可调。
- 构建产物的 `/defects/` 本地预览曾直接打开、刷新并验证标题、Logo 和 WebGL 画布。构建有 Three.js 大包告警，当前未按体积告警宣称性能验收完成。
- 浏览器在热更新中曾记录旧模块导出错误；随后使用全新本地页面复核：首次加载和刷新后的 error/warn 均为空，Logo 自然尺寸大于零、WebGL 画布存在且无横向溢出。刃型滑移实测播放进度前进、暂停值保持 `63`、重置值为 `0`。仍需补齐项目级截图和归一化对照，不能据此宣称完整视觉验收。

## 未关闭项

1. **P0 科学**：双交滑移及四种交割仍是带条件的候选几何；滑移面、线方向、jog/kink 及闭环条件须由学科审核。不得以可播放示意代替准确模型。
2. **P1 科学**：Frank–Read 已修复显著几何跳变，但接触重联和脱环的物理细节仍需按选定教材逐帧审查。Frank 层错环的缺层/闭合为概念几何，仍需学科验收。
3. **P1 Design QA**：已有稳定的参考截图副本，但尚缺本项目实现截图与同视口归一化对照图。前次浏览器工具明确拒绝通过 data URL 导出截图，并禁止换方式绕过；本轮未尝试其他导出途径。缺证据前不能判定视觉验收通过。
4. **P1 发布**：首次版本号、Git 分支、测试及正式部署目录均未确认；入口卡片应保持 soon。
5. **P2 性能**：生产构建主 JS 约 751 kB（gzip 约 205 kB），Vite 提示单文件体积告警；还需在目标设备记录首屏加载与动画帧率。
